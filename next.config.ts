import type { NextConfig } from "next";

// Next.js configuration — vinext reads this for routing and build options.
// Most Next.js config options work here; see Next.js docs for the full list.
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;